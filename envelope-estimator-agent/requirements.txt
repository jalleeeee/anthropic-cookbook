# Envelope Estimator Agent - Dependencies

# Core AI
anthropic>=0.40.0          # Claude API - vision + reasoning
openai>=1.50.0             # OpenAI embeddings for DDC-CWICR search

# Vector database
qdrant-client>=1.12.0      # Qdrant vector search

# PDF processing
pdf2image>=1.17.0          # PDF to images (requires poppler-utils system package)
Pillow>=10.0               # Image processing

# Data
pandas>=2.0                # For DDC-CWICR data analysis
pyarrow>=15.0              # Parquet file support

# HTTP / APIs
requests>=2.31.0           # Google APIs (Solar, Maps, Geocoding)

# GeoTIFF processing (for satellite roof reports)
tifffile>=2024.1.0         # GeoTIFF parsing
numpy>=1.26.0              # Array processing for DSM analysis

# Web application
fastapi>=0.115.0           # Web framework
uvicorn[standard]>=0.30.0  # ASGI server
jinja2>=3.1.0              # HTML templating
python-multipart>=0.0.9    # File upload support

# Utilities
python-dotenv>=1.0.0       # .env file loading
